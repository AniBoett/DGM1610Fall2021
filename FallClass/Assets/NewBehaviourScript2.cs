using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript2 : MonoBehaviour
{
   private int x = 5, y = 6, z = 50;
   Console.WriteLine(x + y + z);
   {
      int myNum = 9; //for single integers
      double myDoubleNum = 8.99; //for decimals
      char myLetter = 'A'; //for single characters
      bool myBoolean = false; //for true false statements
      string myText = "Hello World"; //for multiple word statements
   }
}
