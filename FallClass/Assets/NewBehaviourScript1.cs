using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript1 : MonoBehaviour
{
    private int x = 5;
    private int x = y;
   Console.WriteLine(x == y); // returns False since x and y aren't equal
   Console.WriteLine(x > y); // returns True because 5 is greater than 3
   Console.WriteLine(x != y); // returns True because x and y aren't equal
   Console.WriteLine(x < y); // returns False since 5 is greater than 3
   Console.WriteLine(x >= y); // returns True because 5 is greater than 3
   Console.WriteLine(x ,= y); // returns False because 5 is greater than 3

}
